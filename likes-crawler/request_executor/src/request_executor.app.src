{
  application, request_executor,
  [
    {description, "Snitch Likes Request Executer application"},
    {vsn, "0.0.1"},
    {registered, []},
    {mod, {request_executor, []}},
    {applications, [kernel, stdlib, lager, gproc, gun, amqp_client]},
    {env, []},
    {modules, []}
  ]
}.
