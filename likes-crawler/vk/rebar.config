{erl_opts, [
  debug_info,
  {d, debug},
  {parse_transform, lager_transform}
]}.

{deps, [
  jsone,
  shotgun,
  lager
]}.

{overrides, [
  {override, jsone, [{erl_opts, [{d, 'ENABLE_HIPE'}, inline]}]}
]}.

{shell, [
  {apps, [vk]},
  {config, "./config/app.config"}
]}.
