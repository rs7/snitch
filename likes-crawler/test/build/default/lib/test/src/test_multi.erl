-module(test_multi).

%%% api
-export([start/1]).

%%%===================================================================
%%% api
%%%===================================================================

start(N) ->
  [
    spawn(fun() -> Module:start(N) end)
    ||
    Module <- [a]
  ].
